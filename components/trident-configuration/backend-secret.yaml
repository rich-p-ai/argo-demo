---
# Trident Backend Credentials Secret
# NOTE: This is a TEMPLATE - actual credentials should be managed via External Secrets
# or created manually before applying backend configs
#
# Required keys:
#   - username: ONTAP admin username (typically 'vsadmin' for SVM-level access)
#   - password: ONTAP admin password
#
apiVersion: v1
kind: Secret
metadata:
  name: backend-fsx-ontap-nas-secret
  namespace: trident
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
type: Opaque
stringData:
  # PLACEHOLDER - Replace with actual credentials or use External Secrets
  username: "vsadmin"
  password: "REPLACE_WITH_ACTUAL_PASSWORD"
