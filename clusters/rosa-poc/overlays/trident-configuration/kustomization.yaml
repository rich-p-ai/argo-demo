apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Use the base trident-configuration component
# The component already contains the POC-specific FSx ONTAP backend configs
resources:
  - ../../../../components/trident-configuration
