#------------------------------------------------------------------------------
# ROSA HCP Cluster Configuration: test-non-prod
# Purpose: Test/Non-Production environment cluster
# VPC: ROSA-Non-Prod (Private PrivateLink cluster)
#------------------------------------------------------------------------------
# Estimated Monthly Cost: ~$400/month (single AZ, cost-optimized)
#   - ROSA HCP fee: ~$123/month
#   - 2x m5.xlarge: ~$277/month
#   - Network: Uses existing Transit Gateway (no additional NAT costs)
#------------------------------------------------------------------------------

# Cluster identification
CLUSTER_NAME="test-non-prod"
ENVIRONMENT="test"

# AWS Configuration
AWS_REGION="us-east-1"
AWS_BILLING_ACCOUNT="656113190503"

# Cluster sizing (X-Small - Single AZ for cost optimization)
REPLICAS=2                      # 2 workers in single AZ
INSTANCE_TYPE="m5.xlarge"       # 4 vCPU, 16GB RAM
MULTI_AZ="false"                # Single AZ for test environment

# Autoscaling
ENABLE_AUTOSCALING="true"
MIN_REPLICAS=2
MAX_REPLICAS=4

#------------------------------------------------------------------------------
# Networking - VPC Configuration
#------------------------------------------------------------------------------
# VPC: ROSA-Non-Prod (vpc-0e9f579449a68a005)
# Reference: configs/vpcs/rosa-nonprod-vpc.env
# Note: Private subnets only - requires PrivateLink cluster
#------------------------------------------------------------------------------

# Single AZ configuration - using us-east-1a private subnet
SUBNET_IDS="subnet-008068c7368e05ff9"
MACHINE_CIDR="10.227.96.0/20"
SERVICE_CIDR="172.30.0.0/16"
POD_CIDR="10.128.0.0/14"
HOST_PREFIX=23

# Access - Private cluster (PrivateLink) required for private-subnet-only VPC
PRIVATE_CLUSTER="true"

# OpenShift Version (set specific version or leave empty for latest stable)
OPENSHIFT_VERSION="4.20.8"

# Tags (format: key=value,key=value - no spaces between pairs)
TAGS="environment=test,cost-center=platform-engineering,owner=platform-team,purpose=testing"

# Account settings
ACCOUNT_ROLE_PREFIX="ManagedOpenShift"
FIPS="false"
DISABLE_WORKLOAD_MONITORING="false"
